/**
 * Token definitions.
 */
const TOKENS = {
  STANDARD: {
    PATHS: {
      CORE: {
        COLOR: {
          DECORATIVE: "./core/color/decorative.json",
          FUNCTIONAL: "./core/color/functional.json",
        },
      },
    },
  },
};

module.exports = {
  TOKENS,
};
