/**
 * Supported token categories.
 */
const CATEGORIES = {
  COLOR: "color",
};

/**
 * Specificaions for token serials.
 */
const SERIAL_SPECIFICATIONS = {
  INDENT: 2,
};

/**
 * Formats that a token can exist as.
 */
const TOKEN_FORMATS = {
  STANDARD: "standard",
  AUTOMATED: "automated",
};

module.exports = {
  CATEGORIES,
  SERIAL_SPECIFICATIONS,
  TOKEN_FORMATS,
};
